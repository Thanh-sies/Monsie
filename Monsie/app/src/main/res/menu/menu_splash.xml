<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" tools:context=".SplashActivity">

    <!-- Search, should appear as action button -->
    <item android:id="@+id/action_daily"
        android:title="@string/day"
        app:showAsAction="ifRoom" />

    <item android:id="@+id/action_monthly"
        android:title="@string/month"
        app:showAsAction="ifRoom" />

    <item android:id="@+id/action_year"
        android:title="@string/year"
        app:showAsAction="ifRoom" />

</menu>